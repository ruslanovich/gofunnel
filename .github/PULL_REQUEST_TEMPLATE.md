## Planning & Decisions (required)

- Link to Execution Plan: `docs/03_execution_plans/...`
- ADR link (if decisions made): `docs/05_decisions/...` or `N/A`
- Issue: #

## Research performed (required)

- Local repo search: `yes/no` + 1 line summary
- External docs via context7: `yes/no` + 1 line summary

## Test-first (required)

- Tests-first commit hash: `<fill>`
- Link to commit(s) with tests (optional): `<fill>`
- If tests were not added first, explain why: `<fill>`
- [ ] Tests were added/updated before implementation (first commit in PR or earlier)
- [ ] Negative cases covered (list below)

### Negative cases covered

- authz (`401/403`): `<fill>`
- validation (`400`): `<fill>`
- not_found (`404`): `<fill>`
- revoked/expired (`410`): `<fill>`
- rate_limit (`429`): `<fill>`

### Contracts

- What public contract changed? (routes/status codes/schema/side effects): `<fill>`

## What Changed

- 

## Why

- 

## Changed Files / Modules

- 

## Docs updated (required)

- Docs updated: `yes/no` + which files

## Tests (required)

- Tests: `yes/no` + which checks/tests were run

## Risks

- 
